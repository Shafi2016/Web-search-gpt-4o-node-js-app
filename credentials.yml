
# User credentials
users:  
  - name: Shafi
    username: admin
    password: ${USER_PASSWORD}    # Securely stored in environment variables

# API keys and secrets
api_keys:  
  serpapi_key: ${SERPAPI_KEY}    # Securely stored in environment variables
  openai_key: ${OPENAI_KEY}      # Securely stored in environment variables
